fn enter_code(input_energy: felt252) -> felt252{
    let code = 42;
    return (code);
    }
fn enter_district(code: ByteArray) -> ByteArray{
    let district: ByteArray = "Amireya";
    return (district);
    }    
fn open_chamber(open: bool) -> bool{
   let open: bool = true;
    return (open);
    }    
fn time_chamber(input_energy: u32) -> u32{
    let encapsulated_time = 4;
    let input_energy = 6;
    let seal_result = input_energy + encapsulated_time; 
    return (seal_result);
    }